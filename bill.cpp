#include <iostream>
using namespace std;
int main()
{
    int unit;
    cout << "Enter the no of unit: ";
    cin >> unit;
    int st = 100, rem_unit = 0, bill = 0;
    if (unit <= 100)
    {
        rem_unit = unit - 100;
    }

    return 0;
}